name: Docker Build

# test

on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches:
      - dev_workflows
  workflow_dispatch:

jobs:
  docker-build-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
